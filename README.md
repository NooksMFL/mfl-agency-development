# MFL Agency Development v2
Wallet-first prototype. Enter a Dapper wallet and the app attempts a public MFL roster lookup, creates an ownership baseline, and tracks future development per ownership spell.

Important: the exact current MFL public roster query still needs to be verified against a real wallet. The UI reports the API error rather than silently failing. Once verified, the next step is historical acquisition reconstruction and persistent hosted storage.
